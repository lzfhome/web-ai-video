# 06 Diagnostics：质量自检、常见问题与红线

> 本文件由 06-seedance2-adapter.md 第二批瘦身拆分而来，保留原规则文本。按主路由命中时读取。

---

## 十五、Seedance 2.0 Prompt 质量自检

输出前必须自检：

```text
【Seedance 2.0 适配自检】
0. 是否已推荐 A/B/C 输出模式，并允许用户指定模式；用户未指定时是否默认使用 C 导演轨纯净版，除非命中 A/B 条件：
0.1 是否已说明本段命中的增强项和理由，且未把未命中的增强默认压进普通段落：
0.2 如果本段可启用 SCV 多角度空间包、FF 首帧图、MAP 调度图、SB 空间侧别锁定卡、视频延长，是否已在输出前明确提醒用户：
1. 是否明确生成模式：
2. 是否明确时长、比例、分辨率：
2.1 时长是否在 4-15 秒之间：
2.2 单个提示词单元是否已估算代码框内字数：≤1900 可交付；1901-2000 已先压缩一轮；>2000 已先删低收益后缀、管理话术和重复字段；若 5-7 个完整镜头或高密度 8-10 个短镜头仍超限，是否按镜头边界拆成连续单元，并用段末状态承接下一单元第一帧：
2.3 短视频/竖屏短剧是否已执行节奏档位闸门：是否继承或补判 `电影感中速 / 短视频中高密度 / 高密度动作或强钩子 / 慢节奏但高信息`；镜头数量是否符合对应档位；10 秒以上 1-2 镜是否命中白名单并写清慢镜头功能；未命中时是否补动作标点、道具信息、反应、空间关系或段末钩子镜头：
2.4 有对白时是否已执行台词时长闸门：是否保留原台词；是否按字数、语速、停顿和表演补偿估算最低时长；是否避免超过 6 字/秒硬塞；时长不足时是否优先延长、拆连续单元或建议回 03 精修：
3. 是否建立官方 `@图片/@视频/@音频` 映射，或按接入端需要建立兼容 `@Image/@Video/@Audio` 映射：
3.1 多主体或跨镜头复用时，是否已按固定句式建立人物锚定 `将 @图片N 中的[身份/外观/服装]定义为 <角色名>`，避免 `@图片N` 裸接动词或方位词：
3.2 最终可投喂提示词是否没有裸写 `asset-xxx` 或无解释的内部资产 ID：
4. 每个引用素材是否说明用途：
4.1 如果上游提供 ST/CL/NP，是否把这些编号只放在代码框外管理信息，或在代码框内展开为自然语言：
4.2 最终纯净提示词是否已写清项目风格、人物审美、场景审美、画面质感、当前镜头光影和风格禁止项，而不是只写内部编号：
4.3 如果 ST/CL 光影与当前镜头真实光源冲突，是否完成融合：当前真实光源优先，保留项目质感、反差、颗粒/渲染媒介和 NP 禁止项：
4.4 镜头轨中出现的每个 `@图片/@视频/@音频` 编号是否已在 `素材锚定` 或 `参考/连续性声明` 中定义，且角色、场景、道具、首帧、调度图、视频、音频职责没有混用：
4.5 每一组可投喂提示词 / 每个 Seedance 单元实际使用的图片参考是否不超过 9 张；人物、场景、道具、首帧、调度图、位置参考图、SCV 角度图是否已全部计入；超过时是否已筛选、合并、转文字约束或拆到下一组：
5. 是否最先输出了可直接投喂的字段化纯净提示词：
6. 纯净提示词中是否混入表格、JSON、参数建议、时长、画幅、分辨率、模式、任务类型或解释性文字：
6.1 纯净提示词中是否已清除“未启用 / 不使用 / 不继承 / 无则删除 / 没有则删除 / 不替代 / 普通段不强制”等管理话术：
6.2 如果使用 `全局控制`，是否只写场景、情绪、光影、风格、动作密度和镜头执行原则，没有写 `9:16 / 13-15秒 / 1080p / 参考生视频 / 多模态参考` 等平台参数：
6.3 若使用 C 导演轨纯净版，是否采用 `特别注意 / 素材锚定 / 参考-连续性声明（按需） / 风格锁定（按需） / 场景锚定 / 镜头轨 / 声音轨 / 硬锁 / 段末状态`，而不是旧的 11 字段表格：
6.4 `特别注意` 是否只放最高优先级画面/摄影/表演红线，没有重复声音要求或 BGM 规则：
6.5 `参考/连续性声明` 是否只在命中时输出，并写清参考图、视频参考、首帧、调度、场景角度、道具单实例锁的“只参考什么 / 不参考什么”；`风格锁定` 是否只在 CG/UE5/MetaHuman/动捕/3A 游戏过场/实时渲染/禁止真人质感等命中时输出：
7. 是否有【大分镜】前置信息：事件、场景、人物位置关系、情绪基调：
8. 若使用 C 导演轨纯净版，每条镜头轨是否都有剧情生成的镜头功能标签；若使用 A 标准字段化详细版，每个【分镜】是否都有焦段：
9. C 的 `镜头/焦点` 是否覆盖焦段、光圈、景深、机位、构图、运镜和焦点落点；A 的每个【分镜】是否都有光圈：
9.1 A 的每个【分镜】是否都有景深与焦点锁；C 是否在 `镜头/焦点` 中写清景深与焦点锁：
9.2 景深与焦点锁是否写清浅/中/大景深、焦点起点、焦点落点、前中后景清晰/虚化关系：
9.3 拉焦、跟焦或 rack focus 是否写清转移对象、触发时机、持续时间、最终落点和禁止跑焦对象：
10. A 的每个【分镜】是否都有机位；C 是否在 `镜头/焦点` 中写清机位：
11. A 的每个【分镜】是否都有构图；C 是否在 `镜头/焦点` 中写清构图：
12. A 的每个【分镜】是否都有运镜；C 是否在 `镜头/焦点` 中写清摄影机行为：
13. A 的每个【分镜】是否都有主体动作/表情；C 是否在 `动作/台词` 中写清主体动作和微表情：
13.1 强情绪镜头是否把“悲伤/愤怒/害怕/心动”等标签拆成眼神、眨眼、嘴部、呼吸、身体细节和情绪弧线：
13.2 是否在限制字段写清情绪过度表演禁止项，如不要假哭、不要咆哮、不要夸张皱眉、不要突然失控：
13.3 如果是大哭、怒吼、尖叫、崩溃等 4 级情绪，是否写清 1-3 级铺垫，避免第一帧直接满格爆发：
14. A 的每个【分镜】是否都有光影；C 是否在 `光影/转接` 中写清光影：
14.1 光影是否写清光源来源、主光方向、亮区、暗区、脸部受光、轮廓光/背景分离、色温和明暗反差：
14.2 如果使用丁达尔/雾化光束，是否写清光束来源、介质和落点：
14.3 是否避免只写“电影级布光 / 高级光影 / 氛围感”：
15. A 的每个【分镜】是否都有限制；C 是否在 `光影/转接` 或 `硬锁` 中写清限制：
16. A 的每个【分镜】是否都有可见衔接；C 的每条镜头轨是否都有可见转接或段末画面状态：
16.0 `下一段衔接 / 衔接 / 转接` 是否写成可见动作、视线变化、道具变化、光线变化或段末画面状态，而不是 `接下一大分镜 / 接下一段 / 承接下一段 / 接分镜2`：
16.1 每个 `运镜` 是否已写成摄影机行为锁句，而不是只写“固定 / 手持 / 跟拍 / 退拍 / 高级运镜”：
16.2 摄影机行为是否写清位置、距离、高度、运动方向、稳定程度、速度和起止状态：
16.3 固定镜头是否写清固定位置、看向对象，并禁止漂移、推拉摇移和无动机变焦：
16.4 手持、跟拍、退拍、遮挡转场、rack focus 是否写清跟随对象、强度、方向、停点和禁止项：
16.5 摄影机行为是否和 SCV/MAP/SB 的摄影机安全区、轴线、窗内外/柜台内外/车内外关系一致：
17. 是否已把总时长、画幅、模式和任务类型放在代码框外；正文是否按镜头顺序或软时间节奏组织，而不是默认硬时间戳：
17.1 快切、蒙太奇、广告卡点等是否只使用“每镜约 0.5-1 秒 / 前段中段后段”等软节奏描述，并避免把绝对秒数写成精确控制：
17.2 短视频平台下是否继承 04 的节奏档位判定和镜头密度判断：`短视频中高密度` 或 `高密度动作或强钩子` 是否没有被压回 1-2 镜；10 秒以上只有 1-2 镜时，是否已在代码框外提示慢节奏风险，并在纯净提示词中写清慢镜头功能：
18. 是否保留独立 `台词与语气` 字段，并写清原台词、说话者、语气、音量、语速和口型要求；无对白时是否写明无对白：
18.1 是否没有在 06 中擅自删改、压缩或重写台词；若对白超出建议时长，是否按延长到 15 秒内、按台词节拍拆单元、回 03 精修的顺序处理：
19. 是否写明音频要求（只生成环境声、对白原声和同步音效；默认不生成 BGM；需要时可写画外音、非人声、多人同时出声和音量层级）：
20. 是否写明段末状态，方便下一段延续：
21. 是否存在过多动作或过多场景：
22. 是否把参数建议放在提示词正文之外：
22.1 是否把段号、时长、画幅、分辨率、模式、任务类型等投喂信息放在代码框之外：
23. 是否继承 04 的平台节奏标记：
24. 是否写明本段钩子、信息增量、情绪推进和段末继续看的理由：
25. 慢镜头、空镜、沉默是否都有功能，是否避免无功能拖慢：
25.1 是否避免为了压缩 1900 字而吞掉功能镜头、动作标点、道具信息镜头、反应镜头或段末钩子；字数超限时是否先删低收益后缀和重复字段；仍超限时是否只按镜头边界拆连续单元：
26. 是否保留旧版信息强度：C 用特别注意、素材锚定、参考/连续性声明、风格锁定、场景锚定、镜头轨、声音轨、硬锁、段末状态承载；A 才展开素材、事件交代、场景、人物位置关系、分镜、下一段衔接、通用后缀、段末状态：
27. 如果是强连续段，是否写明本视频第一帧状态、点位表、调度图参考、目标末端状态：
28. 如果是独立批量强连续模式，首帧和限制字段是否已清洗“承接上一段 / 仍然 / 继续 / 同一场景”等依赖上下文的说法：
29. 如果只是普通衔接模式，是否没有强行加入调度图、点位表和禁止错误站位：
29.0 是否已对相邻段边界给出连续性建议：视频延长 / 首帧图 + 调度图 / 只首帧图 / 只调度图 / 普通衔接：
29.0.1 如果建议视频延长，是否确认为同场景顺延，并在纯净提示词最前面写明自然延长 X 秒：
29.0.2 如果切换新场景，是否没有继承上一场的强连续空间、点位表、MAP/SB、SCV 或视频延长逻辑：
29.0.3 如果是场景首现多人调度，是否建议 FF 首帧图 + MAP/SB 调度图：
29.1 最终可投喂提示词是否已把轴线、点位、锚点、MAP/SB 编号转换成 Seedance 可理解的画面语言：
29.2 纯净提示词中若出现 A1/A2/B2/C1、MAP001、SB001、180 度轴线、不要越轴、锚点等词，是否同时写清左/右、前/后、内/外、摄影机侧别、人物站位和移动方向；没有解释的是否删除：
30. 如果 04/05 启用 CR 人群/群演调度卡，是否已把 CR 翻译成当前段可见的 `人群密度` 字段，而不是只写 CR 编号：
31. 启用人群时，是否写清前景/中景/远景人数范围、人群类型、移动方向和遮挡规则：
32. 启用人群时，限制字段是否避免“禁止额外人物”，改写为禁止新增主要角色、路人不看镜头、不挡脸、不抢戏、不同脸复制：
33. 无人群需求时，是否删除 `人群密度` 字段，并在限制里写“不允许出现额外人物、路人、群演”：
34. 使用空场景母版时，是否写清只参考空间结构、灯光和透视；启用人群时是否另用 `人群密度` 正向写清当前段可见人数和调度，而不是写“不继承空无一人状态”：
35. 如果启用 SCV 多角度空间包，是否写清当前段使用哪张 PLAN/V01/V02/V03/V04 角度图：
36. 如果启用 SCV，是否写清该角度图只参考拍摄方向、空间透视、门窗/家具相对位置和固定光源，不参考人物、人群、剧情动作或首帧构图：
37. 如果启用 SCV，是否检查 SCV 与 SC 场景母版、MAP/SB 和 FF 没有冲突；如冲突是否停止输出并要求修正：
38. 普通场景或无反打/回切/高穿帮风险段，是否没有强行加入 `场景角度参考` 行：
39. 如果启用 FF 首帧图，是否在 `素材` 字段最先说明 @图片N（或兼容 @ImageN）是首帧图，且只锁定第 0 秒：
40. 如果启用 FF，是否仍然写清后续动作、运镜、光影变化、限制和段末状态，没有让视频停在首帧：
41. 如果启用 FF，是否同时说明 SC/SCV/CH/PR/MAP/SB 的用途，避免首帧图替代角色、场景、道具和调度参考：
42. FF 与 SCV/MAP/SB/点位表是否一致；如冲突，是否停止输出并要求重做：
43. 如果 SC/SCV/FF 中已经可见某个 PR 道具，同时又引用 PR 道具图，是否已写清道具单实例锁；C 压入 `参考/连续性声明` 和 `硬锁`，A 可单列 `道具单实例锁`：
44. 道具单实例锁是否写清 PR 不是新增道具，只修正已存在那一件道具外观：
45. 道具单实例锁是否写清数量限制、绑定位置、移动后原位置变化和禁止第二个同款道具：
46. 普通衔接段或 C 可不用首帧段，是否没有强行加入 `首帧参考` 行：
47. 首帧相关文字是否已清洗“承接上一段 / 继续 / 仍然 / 同一场景 / 参考 FF001”这类依赖上下文或管理编号的说法：
48. 如果本段是一镜到底、复杂运镜、动作戏、人群调度或强声音叙事，是否已推荐 B 导演执行型或在 A/C 中吸收导演执行增强，并回填到 C 导演轨纯净版或 A 标准字段化详细版结构：
49. 如果本段信息密度高，是否在 1900 字安全红线内优先保留 STYLE LOCK、素材锁定、空间结构、摄影机行为、动作节奏与顺序、声音叙事、限制项，并先去重复再拆段：
```

---

## 十六、常见问题与处理

### 1. 用户只有 04 分镜，没有资产库

先反推临时素材映射：

```text
临时 @图片1 = 当前场景参考图（待用户提供）
临时 @Image2 = 主角参考图（待用户提供）
```

同时提醒：没有参考图时可用文生视频，但角色一致性下降。

### 2. 用户只有资产库，没有分镜

先建议回到 04 生成分镜，再适配 Seedance。  
如果用户要快速输出，则按剧本直接生成“简化 Seedance prompt”，并标注未经过 04 分镜拆解。

### 3. 用户要一次输出整集

不要把整集塞进一个 prompt。必须拆为多个 Seedance 提示词包，每包 4-15 秒。

### 4. 用户问“为什么 Seedance 画面漂移”

优先检查：

- 是否没有角色定稿图
- 是否没有 `@Image` 绑定
- prompt 是否动作过多
- 是否场景切换过多
- 是否没有 Must Keep
- 是否没有末帧延续
- 是否同一角色使用了不同参考图

---

## 十七、常用调用语

```text
把这段 04 分镜转成 Seedance 2.0 提示词包。
```

```text
把这段 04 分镜转成可以直接复制进 Seedance 的纯净提示词。
```

```text
把这段 04 分镜转成 Seedance 2.0 标准字段化纯净提示词，单个单元 4-15 秒，默认控制在 1900 字以内；超过 2000 时先删低收益后缀和重复字段，仍超限则按镜头边界拆成连续单元。
```

```text
把这段 04 分镜转成 06 Seedance 提示词；如果适合 A/B/C 中某一种模式，请先推荐模式；如有强运镜、动作戏、人群、声音叙事或遮挡转场，请启用导演执行型增强。
```

```text
把这段 04 分镜转成 06 导演轨纯净版提示词，保留旧版字段强度，同时去掉重复约束和低收益后缀，单个单元默认控制在 1900 字以内；超过 2000 时按镜头边界拆成连续单元。
```

```text
先判断这段需要哪些增强：视频延长、FF 首帧、MAP/SB 调度、SCV 场景角度、CR 人群、PR 道具单实例锁或复杂运镜，然后推荐 A/B/C 模式并输出 Seedance prompt。
```

```text
检查这段是否需要 FF 首帧图、MAP 调度图或 SB 空间侧别锁定卡，再转 Seedance prompt。
```

```text
根据资产库，把 @图N 改成 @Image/@Video/@Audio，并输出 Seedance prompt。
```

```text
我要用 Seedance 2.0 做竖屏短剧，请按 9:16、每段 4-15 秒输出。
```

```text
给我中英双版 Seedance 2.0 prompt，保留中文台词。
```

```text
检查这个 Seedance prompt 为什么角色会漂移。
```

---

## 十八、红线

1. 不把 04 的分镜表原样当作 Seedance prompt；必须适配为官方 `@图片/@视频/@音频` 或兼容 `@Image/@Video/@Audio`、4-15 秒单元、字段化纯净提示词和 Seedance 可读限制。
2. 不凭空写不存在的 `@图片/@视频/@音频` 或 `@Image/@Video/@Audio`。
3. 不输出低于 4 秒或高于 15 秒的单段 prompt。
4. 不交付超过 2000 字的纯净提示词代码框；内部草稿超过 2000 字时必须先删除低收益后缀、管理话术、重复字段和空泛词。1901-2000 不能直接放行，必须先压缩一轮。若 5-7 个完整镜头或高密度 8-10 个短镜头仍超限，按镜头边界拆成连续 4-15 秒单元，并用上一单元段末状态承接下一单元第一帧。
5. 不省略任一镜头的必填信息：焦段、光圈、景深与焦点锁、机位、构图、运镜、主体动作/表情、台词与语气、光影、限制、可见衔接。C 可合并进镜头轨，A 才逐项展开。
6. 不忽略资产库的状态；未定稿必须标风险。
7. 不删除中文台词的语气和角色身份；有对白时不得只写“说话 / 低声说 / 轻声说”，必须单独保留 `台词与语气` 字段。
7.1 不在 06 中擅自改写、压缩或删减台词；06 只做台词可执行性检查。对白无法在建议时长内以自然语速说完时，不得用超过 6 字/秒的过快语速硬塞；必须延长到 15 秒内、按台词节拍拆连续单元，或建议回 03 台词医生精修。
8. 不只写“电影感、高质量、8K”，必须写主体动作和情绪目标。
8.1 不只写“电影级布光 / 高级光影 / 氛围光”，必须写具体光源、亮暗区域、脸部受光、轮廓光、色温和反差。
8.2 不只写“悲伤 / 愤怒 / 害怕 / 心动”，必须写可见微表情、呼吸和身体动作，并写清禁止过度表演。
8.3 不把“大哭 / 怒吼 / 崩溃”当作起始状态；必须写情绪从克制、压不住到爆发的过程。
8.4 不只写“固定 / 手持 / 跟拍 / 退拍 / 复杂运镜”；必须写成摄影机行为锁句，包含位置、距离、高度、运动方向、稳定程度、速度、起止状态和禁止项。
8.5 不默认在纯净提示词正文里写硬时间戳。总时长、画幅、模式和任务类型放代码框外；正文用镜头顺序和软节奏。用户明确要时间戳时，必须标注为“节奏参考，非精确控制”。
8.6 不把短视频平台 10-15 秒段落默认压成 1-2 个镜头。必须继承或补判节奏档位：电影感中速 / 短视频中高密度 / 高密度动作或强钩子 / 慢节奏但高信息；只有命中慢节奏白名单并写清慢镜头功能时，才允许保留 1-2 镜。不得为了压字数删除镜头密度、台词节拍或段末钩子。
9. 不承诺某个接入端的字段完全通用；平台字段必须按实际文档调整。
10. 不让 `@Video` 参考同时承担太多职责。
11. 不省略保持项（Must Keep）/ 禁止项（Avoid）。
12. 不让下一段丢失上一段的末帧状态。
13. 不把表格、JSON、解释说明、参数建议混进“字段化纯净投喂提示词”代码块。
13.1 不把 `【Seedance 段 Sxx】`、段号、时长、画幅、分辨率、模式、任务类型等给用户确认的投喂信息放进纯净提示词代码框，除非用户明确要求“一键复制完整包，包含元信息”。
13.2 不把“未启用 / 不使用 / 不继承 / 无则删除 / 没有则删除 / 不替代 / 普通段不强制”等管理话术写进纯净提示词代码框；未命中的增强字段直接不输出，说明放代码框外。
13.3 不把 `接下一大分镜 / 接下一段 / 承接下一段 / 接分镜2` 写进纯净提示词；衔接必须改写成模型可生成的可见动作、可见转接或段末状态。
14. 不默认输出只有管理信息、没有可复制正文的“提示词包”。
15. 不为了强连续增强而删掉旧版基础结构；`下一段衔接`、`通用后缀`、`段末状态` 必须保留。
16. 不把强连续规则套到所有段落；普通衔接模式不强制调度图和点位表。
17. 独立批量强连续模式不能只写“承接 B01 / 上一段 / 仍然 / 继续 / 同一场景”，必须改写成当前视频第一帧可见状态。
18. 不把首帧功能套到所有段落；只有 A 必须首帧、B 建议首帧或用户明确要求时才处理首帧。C 压入 `参考/连续性声明`，A 详细版可单列 `首帧参考`。
19. 不把 FF 首帧图当成整段视频控制图；FF 只锁定第 0 秒，后续动作、运镜和段末状态必须继续写。
20. 不让 FF 替代 SC/SCV/CH/PR/MAP/SB；首帧图、场景母版、场景角度图、角色图、道具图、调度图必须职责分离。
21. FF 与 SCV/MAP/SB/点位表冲突时，不输出最终 prompt，先回到 05/04 修正资产。
22. 不隐藏 A/B/C 输出模式。用户要求 Seedance prompt 时，必须先推荐 A 标准字段化详细版、B 导演执行增强版或 C 导演轨纯净版；用户不指定时默认按 C 继续，除非命中 A/B 条件，并在代码框外简短说明本段命中的视频延长、FF、MAP/SB、SCV、CR、PR 或复杂运镜增强。
23. 不在最终字段化纯净投喂提示词里只写 `ST001 / CL001 / NP001 / 继承 ST / 继承 CL / 继承 NP`；必须展开成 Seedance 可理解的项目风格、光影、画面质感和风格禁止项。
24. 不让风格锁覆盖当前镜头真实光源；ST/CL 与本段光影冲突时，以本段可见光源和剧情时段为准，同时保留项目质感、明暗反差、颗粒/渲染媒介和 NP 防跑偏禁止项。
23. 不把导演执行增强套到所有段落。普通段落默认用 C 导演轨纯净版；高难度镜头才推荐 B，或在 C 中吸收对应增强并回填到镜头轨、声音轨、硬锁和段末状态。
24. 不在用户不知道的情况下启用或跳过 SCV/FF/MAP/SB。判断需要多角度空间包、首帧图、调度图、空间侧别锁定时，必须在交接或输出前告诉用户。
25. 不把工程管理标签单独喂给 Seedance：`A2 / B2 / MAP001 / SB001 / 180 度轴线 / 不要越轴 / 锚点` 这类词如果出现，必须同时写成可见画面事实；否则删除。
26. 不把 04b 的内部运镜或动作组合编号写进最终 prompt；只能写具体摄影机行为、动作起手、接触点、受力结果和反应。
27. 不把视频延长套到所有段落；只有同场景顺延、下一段就是上一段动作和镜头的自然继续时才建议。
28. 不在换场景后继承上一场的点位表、MAP/SB、SCV、强连续空间或视频延长逻辑。
29. 不把视频延长启动句当成 `素材：` 字段；启动句放在最前面，后面仍必须保留对应的 06 A/C 输出结构。
30. 不让摄影机行为破坏空间连续：启用 SCV/MAP/SB 时，摄影机不得越过安全区、不得把窗内外/柜台内外/车内外关系拍反、不得在同一段内无动机换到反方向。
31. 不让同一关键道具因为同时引用 SC/SCV/FF 和 PR 图而复制成两个；PR 若只是外观参考，必须写明“不新增实例”。
32. 不裸写 `asset-xxx` 或无解释的内部资产 ID。角色、场景、视频、音频必须通过 `@图片N / @视频N / @音频N`、兼容英文别名、`<角色名>` 或自然语言桥接。
33. 多主体场景不得只靠 `@图片1 / @图片2` 反复裸写；必须按固定句式定义 `<角色名>`，并用身份、外观、服装等稳定静态特征区分人物，避免双胞胎、串脸和数字粘连歧义。
34. 不在单组可投喂提示词中引用超过 9 张图片参考；项目资产库可以超过 9 张，但每个 Seedance 单元最终上传和引用的 `@图片N / @ImageN` 总数必须不超过 9。
35. 编辑视频和延长视频不得写成“参考 @视频N”。编辑用 `严格编辑 @视频N`，延长用 `向后延长 @视频N` 或自然延长启动句。

---

## 输出完整性复核

模块输出完成前，检查最终回复是否已经在全部正文、提示词、表格和代码块结束后，单独追加统一收束声明。该声明只在整次最终回复最后一行出现一次；如果总控或其他模块已经追加，本模块不得重复追加。不得把该声明写入 Seedance 纯净提示词、素材映射、Must Keep、Avoid、参数建议、代码块、表格字段或任何可复制投喂内容中。

资源整理于网络，资料仅用于个人参考学习，请12小时内自行删除，请支持正版。

